'use babel';

import CamelLspClientAtomView from '../lib/camel-lsp-client-atom-view';

describe('CamelLspClientAtomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
